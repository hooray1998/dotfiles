#!/bin/bash

vv()
{
	if [ $# -eq 0 ]
	then
		last=`cat ~/.local/.last_vim`
		if [ -z $last ]
		then
			vim 
		else
			vim $last
		fi
	else
		last=$1;
		echo `pwd`"/$last" > ~/.local/.last_vim
		vim $@
	fi

}
